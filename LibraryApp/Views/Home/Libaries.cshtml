@model IEnumerable<LibraryApp.Models.LibaryModdel>

@{
    ViewData["Title"] = "Libaries";
}

<h1>@("רשימת ספריות ")</h1>

<p>
    <a asp-action="CreatLibary">הוסף ז'אנר</a>
</p>
<table class="table">
    <thead>
        <tr>
            
            
            <th>
                @Html.DisplayNameFor(model => model.genre)
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>            
         
            <td>
                @Html.DisplayFor(modelItem => item.genre)
            </td>
            <td>
                    @Html.ActionLink("הוסף מדף", "CreateShelf", new { id = item.Id }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>
